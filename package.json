{
  "name": "arrdeck",
  "version": "1.0.0",
  "private": true,
  "description": "*Arr Deck - OpenAction multi-service plugin for *arr media management",
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "test:watch": "node --test --watch tests/**/*.test.js"
  }
}
